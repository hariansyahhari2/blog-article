<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<div class="jumbotron text-center">
    <h2>Articles</h2>
</div>

<!--
<div id="myCarousel" class="carousel slide">

  <!-- Indicators ->
  <ul class="carousel-indicators">
    <li class="item1 active"></li>
    <li class="item2"></li>
    <li class="item3"></li>
  </ul>
  
  <!-- The slideshow ->
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img src="" alt="Los Angeles" width="1100" height="500">
    </div>
    <div class="carousel-item">
      <img src="" alt="Chicago" width="1100" height="500">
    </div>
    <div class="carousel-item">
      <img src="">
    </div>
  </div>
  
  <!-- Left and right controls ->
  <a class="carousel-control-prev" href="#myCarousel">
    <span class="carousel-control-prev-icon"></span>
  </a>
  <a class="carousel-control-next" href="#myCarousel">
    <span class="carousel-control-next-icon"></span>
  </a>
</div>
-->



<div class="container" style="margin-top:30px">
<h3>Article posted:</h4>
  <div class="row">
    <div class="col-sm-4">
      <% @articles.each do |article| %>
          <h4><%=article.title %></h4>
          <h5 style ="margin-left: 15px"><%= article.content.truncate(30) %></h5>
          <h6 style ="margin-left: 15px">status : <%= article.status %></h6>
          <span style ="margin-left: 15px"><%= link_to 'read more',  article_path(article.id) %></span>
          <span> | </span>
          <span><%= link_to 'edit', edit_article_path(article.id)%></span>
          <span> | </span>
          <span> <%= link_to 'delete', article_path(article.id), method:"DELETE", data:{confirm:'Delete this data?'} %> </span>
          <p>____________________________________</p>
      <% end %>
    </div>
  </div>
  
  <%= link_to 'Create a new article', new_article_path%>
</div>
</div>